<%= form_with(model: @entidad_responsable, local: true) do |form| %>
  <%= form.text_field :nombre %>
  <%= form.submit "Guardar", class: 'btn btn-success' %>
<% end %>
<%= link_to "Regresar a Catálogo", entidad_responsables_path, class: "btn btn-primary" %>