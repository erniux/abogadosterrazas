<%= form_with(model: @despacho, local: true) do |form| %>	
  <%= form.submit "Guardar", class: 'btn btn-success' %>
<% end %>
<%= link_to "Regresar a Catálogo", despachos_path, class: "btn btn-primary" %>