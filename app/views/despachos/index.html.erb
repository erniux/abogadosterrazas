<h4>Catálogo Ubicaciones</h4>
<div>
	<%= form_tag despachos_path, method: :get do %>
  		<div class="input-group">
      		<%= text_field_tag 'term', params[:term], placeholder: "Ingrese criterio de búsqueda", class: "form-control" %>
      		<span class="input-group-btn">
            	<button class="btn btn-default" type="submit">
                	<i class="glyphicon glyphicon-search"></i>
            	</button>
        	</span>
    	</div><br>
    	<%= link_to new_despacho_path, class: "btn btn-primary", remote: true  do %>
			<i class="glyphicon glyphicon-plus"></i>
				Agregar Ubicaciones
    	<% end %>
  <% end %>
</div>

 
<div class="modal" tabindex="-1" role="dialog" id="form-modal">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="form-modal-title"></h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body" id="form-modal-body">        
      </div>
    </div>
  </div>
</div>

<table class="table">
	<thead>
		<tr>
			<th>Despacho</th>	
			<th>Calle y Número</th>
			<th>Responsable</th>
			<th>Total Ubicaciones</th>
		</tr>
	</thead>
	<tbody>
		<% @despachos.each do |despacho| %>
			<tr>
				<td><%= despacho.colonia %></td>
				<td><%= despacho.calle + ' ' + despacho.numero %></td>
				<td><%= despacho.abogado(despacho.abogado_id) %> </td>
				<td><%= despacho.locations.count %></td>
				<td><%= link_to  edit_despacho_path(despacho), class: "btn btn-info", remote: true  do %>
						<i class="glyphicon glyphicon-pencil"></i>
					<% end %>

					<%= link_to despacho, method: :delete, data:{ confirm: '¿Confirma eliminar el registro?'}, class: "btn btn-danger"  do %>
						<i class="glyphicon glyphicon-minus"></i>
					<% end %>
				</td>
			</tr>
		<% end %>

	</tbody>
</table>