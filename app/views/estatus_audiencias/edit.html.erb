<%= form_with(model: @estatus_audiencia, local: true) do |form| %>
  <%= form.text_field :estatus %>
  <%= form.submit "Guardar", class: 'btn btn-success' %>
<% end %>
<%= link_to "Regresar a Catálogo", estatus_audiencias_path, class: "btn btn-primary" %>